# @screen-reader-mirror/web (planned — M4)

The shareable, zero-install front door: a static site (GitHub Pages) where anyone pastes HTML or a
public URL and sees the side-by-side visual vs. screen-reader interpretation, running the
[core engine](../core/) entirely **in their browser** — nothing is sent to a server.

Placeholder. Defined in the [roadmap](../../docs/project/roadmap.md) (M4); needs a journey + spec
(task T41). A real `build` here (producing `packages/web/dist`) activates the
[`deploy-pages`](../../.github/workflows/deploy-pages.yml) workflow. Not published to npm.
